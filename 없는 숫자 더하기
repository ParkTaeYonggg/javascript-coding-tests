/* 1단계 */

/* 없는 숫자 더하기 : 프로그레머스 */

function solution(numbers) {
    let answer = 0;
    for (let i = 0; i < 10; i++) {
        if (!numbers.find(e => e===i)) {
            answer += i;
        }
    }
    return answer;
}
